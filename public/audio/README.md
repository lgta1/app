# Waifu Summon Background Music

Place `music-nen.mp3` (or another MP3 of your choice) in this folder so the waifu summon page can load `/audio/music-nen.mp3` as background music. The file is not tracked in git to keep the repository lightweight, so remember to deploy the audio manually if needed.
